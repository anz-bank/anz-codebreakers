import styled from 'styled-components'

import { AnzGrid, AnzCol } from '@anz/grid'

export const HardGrid = styled(AnzGrid)`
  margin-top: 2rem;
`

export const HardCol = styled(AnzCol)`
  margin-bottom: 2rem;
`
