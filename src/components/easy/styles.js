import styled from 'styled-components'

import { AnzGrid, AnzCol } from '@anz/grid'

export const EasyGrid = styled(AnzGrid)`
  margin-top: 2rem;
`

export const EasyCol = styled(AnzCol)`
  margin-bottom: 2rem;
`
