import styled from 'styled-components'

import { AnzGrid, AnzCol } from '@anz/grid'

export const HomeCol = styled(AnzCol)`
  margin-bottom: 2rem;
`

export const HomeGrid = styled(AnzGrid)`
  margin-top: 2rem;
`
